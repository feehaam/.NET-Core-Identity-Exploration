namespace IdentityExploration
{
    public class UserRoles
    {
        // 9. These are the roles for our testing, there is no actual use case. 
        public static readonly string[] Roles = { "admin", "user", "shop", "delivery" };
    }
}
